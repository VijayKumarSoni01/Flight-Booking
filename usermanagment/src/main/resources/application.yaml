spring:
  application:
    name: usermanagment

  datasource:
    url: jdbc:mysql://localhost:3306/flightbooking
    username: root
    password: Vijay@@9299
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    

app:
  jwt:
    secret-key: mynameisvijaysonifrompichhari09bokaro
    expiration-time: 180000
    refresh-expiration-time: 300000

logging:
  file:
    name: logs/app.log